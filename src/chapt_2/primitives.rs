#![allow(dead_code)]
/*
pub fn run() {
    // Variables can be type annotated
    // Integers will default to i32, floats to f64
    let my_float: f64 = 1.0; // Regular annotation
    let my_int = 5i32; //Suffix annotation

    // A mutable variable's value can be changed
    let mut mutable = 12;
    mutable = 21;

    // mutable = true; will not work. Type can't be changed!

    // But they can be overwritten with shadowing
    let mutable = true;
}
*/